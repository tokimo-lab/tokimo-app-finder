/**
 * VFS source type labels — matches the web package's settings labels.
 */
export const sourceTypeLabels: Record<string, string> = {
  local: "本地存储",
  smb: "SMB",
  nfs: "NFS",
  ftp: "FTP",
  sftp: "SFTP",
  webdav: "WebDAV",
  s3: "S3",
};
